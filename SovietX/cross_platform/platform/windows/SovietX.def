; ============================================================
; SovietX.def - DLL 导出定义文件
; ============================================================
; 
; 此文件定义了 DLL 导出的函数。
; DllMain 由系统自动识别，不需要显式导出。
; 如果需要导出额外函数供外部调用，在此添加。

; LIBRARY 由 CMake 自动设置，无需指定

EXPORTS
    SovietX_Start
    SovietX_Stop
    SovietX_GetStatus
